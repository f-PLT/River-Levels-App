[project]
name = "river-databases"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.17.2",
    "psycopg[binary]>=3.3.2",
    "river-core",
    "sqlalchemy>=2.0.45",
]

[tool.uv.sources]
river-core = { workspace = true }

[project.scripts]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
